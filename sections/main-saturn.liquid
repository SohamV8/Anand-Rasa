{% render 'planet-product-base', section: section, product: product %}

{% schema %}
{
  "name": "Saturn Energy Attar",
  "settings": [
    { "type": "header", "content": "Brand & hero — Saturn Energy" },
    { "type": "text", "id": "brand_text", "label": "Brand line (above title)", "default": "ANAND RAS" },
    { "type": "text", "id": "product_label", "label": "Product label (hidden in new layout; optional SEO)", "default": "Saturn Energy Attar" },
    { "type": "textarea", "id": "short_description", "label": "Short description (below title)", "info": "Shown under the product title. Quantity appears above Add to cart. Overrides the short line when set.", "default": "A grounding attar crafted for discipline, karma, and stability — supporting patience, structure, and responsibility." },
    { "type": "text", "id": "product_tagline", "label": "Short line (fallback)", "info": "Used if the textarea above is empty. Then metafield, then product excerpt.", "default": "Responsibility • Patience • Structure" },
    { "type": "range", "id": "rating", "label": "Star rating (display)", "min": 1, "max": 5, "step": 1, "default": 5 },
    { "type": "text", "id": "review_count", "label": "Review count", "default": "0" },
    { "type": "checkbox", "id": "show_buy_now", "label": "Show Buy Now button", "default": true },
    { "type": "text", "id": "trust_1", "label": "Trust line 1 — Saturn", "default": "Disciplined mindset" },
    { "type": "text", "id": "trust_2", "label": "Trust line 2 — Saturn", "default": "Stable grounding" },
    { "type": "text", "id": "trust_3", "label": "Trust line 3 — Saturn", "default": "Patient endurance" },
    { "type": "header", "content": "Info strip (3 columns) — Saturn Energy" },
    { "type": "text", "id": "strip_col1", "label": "Column 1 — Description", "default": "Discipline and stability" },
    { "type": "text", "id": "strip_col2", "label": "Column 2 — Details", "default": "A steady aroma to support karma work, routine discipline, and long-term structure." },
    { "type": "text", "id": "strip_col3", "label": "Column 3 — Category", "default": "Planet Energy Attar" },
    { "type": "header", "content": "Why choose section — Saturn Energy" },
    { "type": "text", "id": "why_heading", "label": "Heading", "default": "Why Choose Saturn Energy Attar" },
    { "type": "textarea", "id": "why_sub", "label": "Subheading", "default": "Crafted to support responsibility, patience, and structure for grounded progress over time." },
    { "type": "header", "content": "Specifications table — Saturn Energy" },
    { "type": "text", "id": "spec_origin", "label": "Country of Origin", "default": "India" },
    { "type": "text", "id": "spec_item_form", "label": "Item Form", "default": "Luxury Attar" },
    { "type": "text", "id": "spec_item_type", "label": "Focus", "default": "Responsibility, Patience, Structure" },
    { "type": "text", "id": "spec_sticks", "label": "Ideal for", "default": "Routine discipline, deep work, and long-term commitments" },
    { "type": "text", "id": "spec_burn", "label": "Longevity", "default": "Long-lasting grounding depth" },
    { "type": "text", "id": "spec_length", "label": "Application", "default": "Dab on pulse points before focused work" },
    { "type": "text", "id": "spec_fragrance", "label": "Fragrance (optional row)", "default": "Dark woods • earthy resin • subtle spice" },
    { "type": "text", "id": "spec_ingredients", "label": "Key ingredients (optional row)", "default": "Natural resins, essential oils, premium aromatic botanicals" },
    { "type": "header", "content": "Recommended products — Saturn Energy" },
    { "type": "collection", "id": "related_collection", "label": "Collection" },
    { "type": "text", "id": "related_heading", "label": "Heading", "default": "You May Also Like" },
    { "type": "text", "id": "related_sub", "label": "Subheading", "default": "More planetary aromas for your ritual" }
  ],
  "presets": [{ "name": "Saturn Energy Attar" }],
  "enabled_on": { "templates": ["product"] }
}
{% endschema %}
